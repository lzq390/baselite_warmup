# Stage C non-vocab smoke cloud dependencies.
#
# Install a CUDA-enabled PyTorch build first, using the command that matches the
# server CUDA driver/runtime. Example for CUDA 12.1:
#   pip install --index-url https://download.pytorch.org/whl/cu121 torch
#
# Then install the rest:
#   pip install -r requirements-stage-c.txt
#
# Stage C uses a pure PyTorch graph encoder. Do not install PyG or DGL for this
# smoke run unless a later plan explicitly changes the graph stack.

transformers>=4.40
peft>=0.10
accelerate>=0.29
pyyaml>=6.0
rdkit>=2023.9
safetensors>=0.4
huggingface_hub>=0.23
pytest>=8.0
